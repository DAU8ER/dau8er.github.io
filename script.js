document.getElementById("tf").addEventListener("click", () => {
    scrollTo({top: 0, behavior: "smooth"})
});

// let mdata = require("https://modlookup.3v.fi/api/user-totals/Dau8er")
//window.onscroll = (ev) => {
    // console.log(`${scrollY} ${window.innerHeight} ${window.outerHeight-scrollY}\t${window.innerWidth} ${window.innerHeight} ${window.innerWidth/window.innerHeight} ${Math.round(window.innerWidth/window.innerHeight) }`)
   // let scrollnum = (Math.round(window.innerWidth/window.innerHeight) === 2 ? 80 : 210);
    // 207
    //if(window.outerHeight-scrollY <= scrollnum){
      //  console.log(":tf:")
        //scrollTo({top:0, behavior: "smooth"});
    //}
